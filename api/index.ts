import { createApp } from '../backend/src/app'

// Compatibility entry for a backend Vercel project rooted at the repository.
// If the project root is set to backend, backend/api/index.ts is used instead.
export default createApp()
